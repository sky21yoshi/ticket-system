markdown_content = """# チケット管理システム 機能一覧・詳細仕様書

## 1. 概要
本仕様書は、Java (Spring Boot) および PostgreSQL で構築する Redmine 風チケット管理システムの機能一覧および各機能の詳細仕様をまとめたものです。

---

## 2. システム機能一覧 (Function List)

| 大項目 | 中項目 | 小項目 / 機能名 | 概要 | 優先度 |
|---|---|---|---|---|
| **1. 認証・アクセス制御** | 認証 | ユーザーログイン / ログアウト | JWT またはセッションによる認証 | 必須 |
| | | パスワードリセット | メールリンクによるトークン認証と再設定 | 推奨 |
| | 権限管理 | ロールベースアクセス制御 (RBAC) | システム管理者、プロジェクト管理者、一般メンバー、閲覧者 | 必須 |
| **2. プロジェクト管理** | プロジェクト操作 | プロジェクト作成 / 編集 / 削除 | プロジェクト識別子、説明、公開/非公開の設定 | 必須 |
| | | メンバー配属・ロール割り当て | ユーザーごとのプロジェクト内役割定義 | 必須 |
| **3. チケット管理** | チケット CRUD | チケット新規作成 | トラッカー、題名、説明、担当者、優先度、期日 | 必須 |
| | | チケット詳細表示 | ステータス、属性、説明、変更履歴の一覧化 | 必須 |
| | | チケット更新 | 属性変更およびコメント追加 | 必須 |
| | | チケット削除 | チケットおよび関連データの削除 | 必須 |
| | チケット検索・絞り込み | カスタムフィルタ・並び替え | ステータス、担当者、トラッカー等での絞り込み | 必須 |
| | 追跡・履歴 | 変更履歴 (Journal) 自動記録 | どの属性が誰によっていつ変更されたかの自動記録 | 必須 |
| **4. ワークフロー管理** | 状態遷移 | ステータス遷移制御 | トラッカー・ロールに応じたステータス変更制限 | オプション |
| **5. ダッシュボード・集計** | ビジュアル | ガントチャート / マイページ | プロジェクト全体の進行状況可視化、担当チケット一覧 | オプション |
| **6. 通知機能** | メール・Web | チケット変更通知 | 担当者変更やステータス更新時のメール送信 | 推奨 |

---

## 3. 機能詳細仕様 (Detailed Function Specifications)

### 3.1. 認証・権限管理 (Authentication & Authorization)

#### F-101: ユーザーログイン・ログアウト
* **目的**: システムへのアクセス制御とユーザー特定を行う。
* **入力項目**: メールアドレス（またはユーザー名）、パスワード
* **処理内容**:
  1. 入力された資格情報をデータベースのハッシュ化パスワード (`BCryptPasswordEncoder`) と照合。
  2. 認証成功時、JWT (JSON Web Token) または HTTP セッションを発行。
  3. ログイン失敗時は適切なエラーメッセージを表示。

#### F-102: ロールベースアクセス制御 (RBAC)
* **目的**: ユーザーの役割に応じた操作制限を実施する。
* **ロール種別**:
  * **System Admin**: 全プロジェクトの参照・編集・削除、ユーザー管理。
  * **Project Admin**: 担当プロジェクトの設定、メンバー追加・ロール設定。
  * **Developer**: チケットの作成・更新・自身への割り当て。
  * **Reporter**: チケットの作成・閲覧・コメント投稿のみ。
  * **Viewer**: チケットの閲覧のみ。

---

### 3.2. プロジェクト管理 (Project Management)

#### F-201: プロジェクト作成・編集
* **目的**: チケットを分類・管理するためのプロジェクト定義。
* **入力項目**:
  * プロジェクト名 (`name`)
  * プロジェクト識別子 (`identifier`: URL等で使用する英数字記号)
  * 説明 (`description`)
  * 公開区分 (`is_public`: 全ユーザーに公開するかどうか)
* **処理内容**:
  1. 識別子の重複チェック。
  2. プロジェクトデータの保存。
  3. 作成者をデフォルトのプロジェクト管理者として登録。

---

### 3.3. チケット管理 (Issue Management)

#### F-301: チケット新規作成
* **目的**: タスク、バグ、機能要望等をチケットとして登録する。
* **入力項目**:
  * トラッカー (Bug, Feature, Task)
  * 題名 (`subject`)
  * 説明 (`description`: Markdown形式)
  * ステータス (初期値: 新規)
  * 優先度 (低, 通常, 高, 緊急)
  * 担当者 (`assignee_id`: 任意)
  * 開始日・期日 (`start_date`, `due_date`)
  * 予定工数 (`estimated_hours`)
* **バリデーション**:
  * 題名は必須 (最大255文字)。
  * 期日は開始日以降の日付であること。

#### F-302: チケット更新 & 変更履歴 (Journal) 自動記録
* **目的**: チケットの進捗状況を更新し、すべての変更ログを保持する。
* **処理内容**:
  1. チケットの属性値を更新。
  2. 変更前のデータと変更後のデータを比較。
  3. 変更があった項目ごとに `journal_details` テーブルに変更ログを出力：
     * 変更項目名 (`property`)
     * 変更前 (`old_value`)
     * 変更後 (`new_value`)
  4. コメントが入力されている場合は `journals` テーブルにコメント本文と投稿者を記録。

#### F-303: チケット一覧・絞り込み検索
* **目的**: 必要なチケットを迅速に検索・表示する。
* **検索条件**:
  * キーワード検索（題名・説明）
  * プロジェクト ID
  * ステータス ID
  * トラッカー ID
  * 担当者 ID / 作成者 ID
* **レスポンス**: ページネーション対応済みのチケットオブジェクト配列（デフォルト20件/ページ）。

---

### 3.4. ワークフロー管理 (Workflow Management)

#### F-401: ステータス遷移制御
* **目的**: 不正なステータス変更を防止し、業務プロセスを強制する。
* **仕様**:
  * `workflows` テーブルで `(role_id, tracker_id, old_status_id, new_status_id)` の組み合わせを管理。
  * ユーザーがステータスを変更しようとした際、現在割り振られているロールでその遷移が許可されているかを判定。
  * 許可されていない遷移の場合は HTTP 403 (Forbidden) を返却。

---
